<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- Tree (List) View -->
        <record id="view_student_tree" model="ir.ui.view">
            <field name="name">student.student.list</field>
            <field name="model">student.student</field>
            <field name="arch" type="xml">
                <list string="Students">
                    <field name="name"/>
                    <field name="age"/>
                    <field name="grade"/>
                    <field name="enrolled_date"/>
                    <field name="active"/>
                </list>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_student_form" model="ir.ui.view">
            <field name="name">student.student.form</field>
            <field name="model">student.student</field>
            <field name="arch" type="xml">
                <form string="Student Form">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="age"/>
                            <field name="grade"/>
                            <field name="enrolled_date"/>
                            <field name="active"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action -->
        <record id="action_student" model="ir.actions.act_window">
            <field name="name">Students</field>
            <field name="res_model">student.student</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Menu Items -->
        <menuitem id="menu_student_root" name="Student Management"/>
        <menuitem id="menu_student" parent="menu_student_root" action="action_student" name="Students"/>

    </data>
</odoo>
